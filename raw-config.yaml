volume: harpoon-volume # this needs to match the volume name passed to the podman run command
targets:
- name: harpoon
  url: https://github.com/cooktheryan/harpoon-demo
  methods:
    raw:
      targetPath: raw
      schedule: "*/1 * * * *"
      pullImage: true
  branch: main
